# Copy to .env.local and adjust for your environment.
# API URL is centralized in src/config/api.ts — this env var overrides the default.
VITE_API_BASE_URL=http://localhost:8081/api/v1
# Set to true to run the UI without Spring Boot (uses mockData.ts)
VITE_USE_MOCK=false
